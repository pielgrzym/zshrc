#!/bin/sh

. git-sh-setup  # for die 
git status --porcelain | while IFS= read -r line;
do
        f=${line#???}
        case ${f} in
                *.less)
                        echo "Yess, compiling less into css."
                        lessc static/less/bootstrap.less static/css/bootstrap.css
                        fb=${f%.less}
                        git add static/css/bootstrap.css
                        ;;
        esac
done

:
